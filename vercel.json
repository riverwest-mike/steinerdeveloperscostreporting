{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then echo 'Skipping deploy: non-main branch (see CLAUDE.md policy).' && exit 1; fi && npm install",
  "crons": [
    {
      "path": "/api/cron/daily-sync",
      "schedule": "0 6 * * *"
    }
  ]
}
